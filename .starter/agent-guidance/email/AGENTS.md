# Email workspace

Read the repository-level `AGENTS.md` before changing this workspace.

- Keep templates compatible with React Email and the rendering limits of major mail clients.
- Preserve plain-text meaning, descriptive links, and useful content when images are blocked.
- Keep authentication and transactional copy concise and specific.
- Run this workspace's `typecheck` and relevant tests after changes.
