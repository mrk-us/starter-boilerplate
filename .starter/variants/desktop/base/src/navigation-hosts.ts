/**
 * External hosts the desktop shell may open inside the application window.
 * The unauthenticated shell does not need to navigate away from its app URL.
 */
export const ALLOWED_NAVIGATION_HOSTS: readonly string[] = [];
