/**
 * Hosts and subdomains the window may navigate to during WorkOS AuthKit
 * sign-in, including its hosted pages and configured OAuth providers.
 */
export const ALLOWED_NAVIGATION_HOSTS = [
  "workos.com",
  "authkit.app",
  "accounts.google.com",
  "github.com",
];
